#include "framework.h"

int test_function(int *matrix, int n)
{
    /*static int m = 0;
    m++;
    printf("#%d\n", m);
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            printf("%d ", matrix[i * n + j]);
        }
        putchar('\n');
    }
    putchar('\n');*/
    return 1;
}